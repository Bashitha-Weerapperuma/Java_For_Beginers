public class short_ifElse {
    public static void main(String[] args) {
        int marks = 80;
        // condition ? True : False
        String result = (marks >50) ? "Pass" : "Fail";
        System.out.println(result);
    }
}
